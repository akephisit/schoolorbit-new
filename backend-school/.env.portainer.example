# ============================================
# Backend-School Environment Variables
# สำหรับ Portainer Stack
# ============================================

# 1. Admin Database (REQUIRED!)
# ใช้สำหรับ query school mapping (subdomain → database URL)
# ต้องชี้ไปที่ backend-admin database
ADMIN_DATABASE_URL=postgresql://neondb_owner:YOUR_NEON_PASSWORD@ep-xxx-xxx.us-east-1.aws.neon.tech/schoolorbit_admin?sslmode=require

# 2. Internal API Secret (REQUIRED!)
# ต้องตรงกับค่าใน backend-admin
INTERNAL_API_SECRET=your-internal-api-secret-change-this

# 3. JWT Secret (REQUIRED!)
# สำหรับ sign และ verify JWT tokens
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# 4. Server Configuration (Optional - มี default)
RUST_LOG=info
HOST=0.0.0.0
PORT=8081

# ============================================
# ⚠️ สิ่งที่ไม่ต้องตั้งอีกต่อไป:
# ============================================
# DATABASE_URL - ไม่ต้องใช้แล้ว (multi-tenant architecture)
# ALLOWED_ORIGINS - เป็นของ backend-admin เท่านั้น

# ============================================
# 📝 วิธีใช้ใน Portainer:
# ============================================
# 1. ไปที่ Stacks → เลือก backend-school stack
# 2. คลิก "Editor"
# 3. เลื่อนลงไปที่ "Environment variables"
# 4. วาง environment variables ข้างบนนี้
# 5. แก้ไขค่าจริง (ADMIN_DATABASE_URL, secrets)
# 6. คลิก "Update the stack"
