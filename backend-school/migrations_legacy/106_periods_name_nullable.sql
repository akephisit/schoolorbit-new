-- ============================================
-- academic_periods: name เป็น nullable
-- ============================================
-- เหตุผล: ส่วนใหญ่คาบเรียนปกติไม่ต้องตั้งชื่อ (แสดงแค่เวลาก็พอ)
-- เก็บ name ไว้สำหรับเคสพิเศษ เช่น "พักเที่ยง", "โฮมรูม", "กิจกรรม"
-- ============================================

ALTER TABLE academic_periods
    ALTER COLUMN name DROP NOT NULL;
