# Server Configuration
HOST=0.0.0.0
PORT=8081
RUST_LOG=info

# Internal API Authentication (shared secret with backend-admin)
# ⚠️ REQUIRED - ต้องเหมือนกับ backend-admin
INTERNAL_API_SECRET=your-internal-secret-change-this-in-production

# JWT Configuration (ถ้าจะมี authentication ใน backend-school)
JWT_SECRET=your-secret-key-change-this-in-production

# ===================================================================
# Database Configuration (OPTIONAL - สำหรับ testing/development เท่านั้น)
# ===================================================================
# Backend-school ไม่ได้ใช้ DATABASE_URL ใน production
# เพราะจะรับ tenant database connection strings จาก backend-admin แทน
# 
# ใช้เฉพาะเมื่อ:
# - รัน integration tests
# - ทดสอบ migrations ใน local
# 
# DATABASE_URL=postgres://user:password@localhost:5432/school_db
