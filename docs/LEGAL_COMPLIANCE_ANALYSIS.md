# 📋 การวิเคราะห์ความสอดคล้องกับกฎหมาย PDPA - SchoolOrbit

**วันที่วิเคราะห์:** 8 มกราคม 2026  
**ผู้วิเคราะห์:** System Legal Compliance Review  
**เวอร์ชัน:** 1.0  
**ขอบเขต:** Frontend-School, Backend-School

---

## 🎯 สรุปผลการวิเคราะห์

ระบบ SchoolOrbit มีพื้นฐานด้านความปลอดภัยที่ดี แต่ **ยังไม่พร้อมใช้งานตามกฎหมาย PDPA (พ.ร.บ. คุ้มครองข้อมูลส่วนบุคคล พ.ศ. 2562)** เนื่องจากขาดกลไกสำคัญหลายอย่างที่กฎหมายกำหนด

**ระดับความเสี่ยง:** 🔴 **สูง (High Risk)**  
**ความพร้อมโดยรวม:** 35/100

---

## ⚖️ ข้อกำหนดของกฎหมาย PDPA

### 1. หลักการสำคัญของ PDPA

```
✅ = มีอยู่แล้ว
⚠️ = มีบางส่วน ต้องปรับปรุง
❌ = ยังไม่มี ต้องเพิ่ม

⚠️ 1. ความยินยอม (Consent)
❌ 2. จำกัดวัตถุประสงค์ (Purpose Limitation)
⚠️ 3. ความถูกต้อง (Data Accuracy)
❌ 4. การเก็บรักษาอย่างจำกัด (Storage Limitation)
⚠️ 5. ความปลอดภัย (Security)
❌ 6. สิทธิของเจ้าของข้อมูล (Data Subject Rights)
```

---

## 🔍 การวิเคราะห์รายละเอียด

### ภาพรวมข้อมูลส่วนบุคคลในระบบ

#### 📊 ข้อมูลที่เก็บในฐานข้อมูล

**ตาราง `users`:**
```sql
- national_id (เลขบัตรประชาชน) ⚠️ ข้อมูลส่วนบุคคลที่ละเอียดอ่อน
- email
- password_hash ✅
- first_name, last_name
- phone
- date_of_birth ⚠️ ข้อมูลส่วนบุคคลที่ละเอียดอ่อน
- gender ⚠️ ข้อมูลส่วนบุคคลที่ละเอียดอ่อน
- address
- emergency_contact
- line_id
- profile_image_url
```

**ตาราง `student_info`:**
```sql
- student_id
- parent_id
- blood_type ⚠️ ข้อมูลสุขภาพ - ข้อมูลที่ละเอียดอ่อน
- allergies ⚠️ ข้อมูลสุขภาพ - ข้อมูลที่ละเอียดอ่อน
- medical_conditions ⚠️ ข้อมูลสุขภาพ - ข้อมูลที่ละเอียดอ่อน
```

**ตาราง `staff_info`:**
```sql
- employee_id
- teaching_license_number
- education_level
- major
- university

หมายเหตุ: ระบบไม่เก็บข้อมูลการเงินของบุคลากร (เงินเดือน, บัญชีธนาคาร, 
เลขประจำตัวผู้เสียภาษี, เลขประกันสังคม) เพื่อลดความเสี่ยงตาม PDPA และ
แยกระบบการเงินออกต่างหากจากระบบโรงเรียน
```

**ตาราง `audit_logs`:**
```sql
- user_id
- user_email
- ip_address ⚠️ ข้อมูลการติดตาม
- user_agent
- old_values (JSONB) ⚠️ อาจมีข้อมูลส่วนบุคคล
- new_values (JSONB) ⚠️ อาจมีข้อมูลส่วนบุคคล
```

---

## ❌ สิ่งที่ขาดและต้องเพิ่ม (Critical Gaps)

### 1. ⚠️ **ระบบขอความยินยอม (Consent Management)** - Priority: 🔴 สูงสุด

**สถานะปัจจุบัน:** ❌ ไม่มีเลย

**ความเสี่ยง:**
- ผิด PDPA มาตรา 19 (การเก็บรวบรวมข้อมูลโดยไม่ได้รับความยินยอม)
- เสี่ยงถูกปรับสูงสุด 5 ล้านบาท

**สิ่งที่ต้องทำ:**

#### 1.1 เพิ่มตาราง Consent Management
```sql
-- Migration: consent_management.sql
CREATE TABLE consent_records (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    
    -- ผู้ให้ความยินยอม
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    user_type VARCHAR(50) NOT NULL, -- student, staff, parent
    
    -- ประเภทความยินยอม
    consent_type VARCHAR(100) NOT NULL,
    -- เช่น: 'data_collection', 'data_usage', 'marketing', 'third_party_sharing'
    
    -- รายละเอียดความยินยอม
    purpose TEXT NOT NULL, -- วัตถุประสงค์ในการเก็บข้อมูล
    data_categories JSONB NOT NULL, -- ประเภทข้อมูลที่ขอความยินยอม
    
    -- สถานะความยินยอม
    consent_status VARCHAR(20) NOT NULL, -- 'granted', 'denied', 'withdrawn'
    granted_at TIMESTAMPTZ,
    withdrawn_at TIMESTAMPTZ,
    expires_at TIMESTAMPTZ, -- วันหมดอายุความยินยอม (สำหรับกรณีที่มีกำหนด)
    
    -- บริบทการให้ความยินยอม
    consent_method VARCHAR(50) NOT NULL, -- 'web_form', 'paper_form', 'verbal'
    ip_address INET,
    user_agent TEXT,
    consent_text TEXT, -- ข้อความที่แสดงเมื่อขอความยินยอม
    consent_version VARCHAR(20), -- เวอร์ชันของข้อความความยินยอม
    
    -- ข้อมูลเพิ่มเติม
    minor_consent BOOLEAN DEFAULT false, -- ความยินยอมสำหรับผู้เยาว์
    parent_guardian_id UUID REFERENCES users(id), -- ผู้ปกครองที่ให้ความยินยอมแทน
    
    notes TEXT,
    metadata JSONB DEFAULT '{}',
    
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_consent_user_id ON consent_records(user_id);
CREATE INDEX idx_consent_type ON consent_records(consent_type);
CREATE INDEX idx_consent_status ON consent_records(consent_status);
CREATE INDEX idx_consent_user_type ON consent_records(user_id, consent_type, consent_status);

COMMENT ON TABLE consent_records IS 'บันทึกความยินยอมในการเก็บและใช้ข้อมูลส่วนบุคคล (PDPA Compliance)';
```

#### 1.2 เพิ่ม API Endpoints สำหรับ Consent

**Backend (`backend-school/src/handlers/consent.rs`):**
```rust
// POST /api/consent - บันทึกความยินยอม
// GET /api/consent/:user_id - ดูความยินยอมทั้งหมดของผู้ใช้
// PUT /api/consent/:id/withdraw - ถอนความยินยอม
// GET /api/consent/types - รายการประเภทความยินยอม
```

#### 1.3 Frontend - Consent Form

**หน้าลงทะเบียน/เข้าสู่ระบบ:**
```typescript
// ✅ ต้องแสดงข้อความขอความยินยอมก่อนเก็บข้อมูล
- [ ] Checkbox: ยินยอมให้เก็บข้อมูลส่วนบุคคล
- [ ] Link: นโยบายความเป็นส่วนตัว (Privacy Policy)
- [ ] Link: เงื่อนไขการให้บริการ (Terms of Service)
- [ ] ต้องติ๊กถูกเพื่อดำเนินการต่อ (Explicit Consent)
```

**ตัวอย่างข้อความ:**
```
☐ ข้าพเจ้ายินยอมให้โรงเรียน [ชื่อโรงเรียน] เก็บรวบรวม ใช้ และเปิดเผยข้อมูลส่วนบุคคลของข้าพเจ้า 
   ตามวัตถุประสงค์ที่ระบุในนโยบายความเป็นส่วนตัว
   
   อ่าน: นโยบายความเป็นส่วนตัว | เงื่อนไขการให้บริการ
```

**ความยินยอมสำหรับนักเรียนผู้เยาว์:**
```
☐ ข้าพเจ้า (ผู้ปกครอง) ยินยอมให้เก็บข้อมูลส่วนบุคคลของบุตร/ธิดา
   ชื่อผู้ปกครอง: _________________
   ความสัมพันธ์: [ ] บิดา [ ] มารดา [ ] ผู้ปกครอง
```

---

### 2. ❌ **นโยบายความเป็นส่วนตัว (Privacy Policy)** - Priority: 🔴 สูงสุด

**สถานะปัจจุบัน:** ❌ ไม่มีเลย

**ความเสี่ยง:**
- ผิด PDPA มาตรา 23 (แจ้งรายละเอียดการเก็บข้อมูล)
- ไม่สามารถพิสูจน์ความโปร่งใสได้

**สิ่งที่ต้องทำ:**

#### 2.1 สร้างหน้า Privacy Policy

**เส้นทาง:** `/privacy-policy`

**เนื้อหาที่ต้องมี (PDPA มาตรา 23):**

```markdown
# นโยบายความเป็นส่วนตัว (Privacy Policy)

วันที่มีผลบังคับใช้: [วันที่]
อัปเดตล่าสุด: [วันที่]

## 1. ข้อมูลที่เราเก็บรวบรวม

เราเก็บข้อมูลส่วนบุคคลต่อไปนี้:

### 1.1 ข้อมูลส่วนบุคคลทั่วไป
- ชื่อ-นามสกุล
- เลขบัตรประชาชน (สำหรับการระบุตัวตน)
- อีเมล
- หมายเลขโทรศัพท์
- ที่อยู่
- วันเดือนปีเกิด

### 1.2 ข้อมูลส่วนบุคคลที่ละเอียดอ่อน (Sensitive Personal Data)
- ข้อมูลสุขภาพ (กรุ๊ปเลือด, อาการแพ้, โรคประจำตัว - สำหรับนักเรียน)

หมายเหตุ: ระบบโรงเรียนไม่เก็บข้อมูลการเงินของบุคลากร

## 2. วัตถุประสงค์ในการเก็บข้อมูล

เราเก็บข้อมูลของท่านเพื่อวัตถุประสงค์ต่อไปนี้:

### 2.1 สำหรับนักเรียน
- การจัดการการเรียนการสอน
- การติดตามผลการเรียน
- การติดต่อสื่อสาร
- การดูแลสุขภาพและความปลอดภัย
- การรายงานต่อหน่วยงานราชการที่เกี่ยวข้อง

### 2.2 สำหรับบุคลากร
- การบริหารจัดการบุคลากร
- การจัดตารางสอน
- การติดต่อสื่อสาร

## 3. ระยะเวลาในการเก็บรักษาข้อมูล

เราจะเก็บข้อมูลของท่านตามระยะเวลาต่อไปนี้:

- **นักเรียน:** ตลอดระยะเวลาการศึกษา + 5 ปีหลังสำเร็จการศึกษา
- **บุคลากร:** ตลอดระยะเวลาการจ้างงาน + 10 ปีหลังสิ้นสุดการจ้าง
- **audit_logs:** 7 ปี
- **consent_records:** ถาวร (เพื่อพิสูจน์การปฏิบัติตามกฎหมาย)

## 4. การเปิดเผยข้อมูล

เราอาจเปิดเผยข้อมูลของท่านให้กับ:

- หน่วยงานราชการที่เกี่ยวข้อง (สพฐ., กระทรวงศึกษาธิการ)
- สถาบันการเงิน (สำหรับการจ่ายเงินเดือน)
- ผู้ให้บริการภายนอก (Cloud hosting, Email service)

**หมายเหตุ:** เราจะไม่ขายหรือเช่าข้อมูลของท่านให้บุคคลที่สาม

## 5. สิทธิของท่าน (Data Subject Rights)

ท่านมีสิทธิตามกฎหมาย PDPA ดังนี้:

1. **สิทธิในการเข้าถึงข้อมูล** - ขอดูข้อมูลของท่านได้
2. **สิทธิในการแก้ไข** - เปลี่ยนแปลงข้อมูลที่ไม่ถูกต้อง
3. **สิทธิในการลบข้อมูล** - ขอลบข้อมูลในบางกรณี
4. **สิทธิในการระงับการใช้** - ขอให้หยุดใช้ข้อมูลชั่วคราว
5. **สิทธิในการโอนย้ายข้อมูล** - ขอรับสำเนาข้อมูลในรูปแบบอิเล็กทรอนิกส์
6. **สิทธิในการคัดค้าน** - คัดค้านการประมวลผลข้อมูล
7. **สิทธิในการถอนความยินยอม** - ถอนความยินยอมได้ทุกเวลา

### วิธีการใช้สิทธิ:
- ติดต่อ: dpo@[โรงเรียน].ac.th
- โทร: 0X-XXXX-XXXX
- หรือผ่านระบบ: เมนู "ข้อมูลส่วนตัว" → "สิทธิของฉัน"

## 6. ความปลอดภัยของข้อมูล

เราใช้มาตรการรักษาความปลอดภัยดังนี้:

- เข้ารหัสข้อมูล (Encryption)
- ระบบ Authentication และ Authorization
- Audit Logging
- Backup ข้อมูลสม่ำเสมอ
- จำกัดการเข้าถึงข้อมูล (Role-based access)

## 7. การติดต่อ

หากท่านมีคำถามเกี่ยวกับนโยบายนี้ กรุณาติดต่อ:

**เจ้าหน้าที่คุ้มครองข้อมูลส่วนบุคคล (DPO)**  
ชื่อ: [ชื่อ DPO]  
อีเมล: dpo@[โรงเรียน].ac.th  
โทร: 0X-XXXX-XXXX  

---

อัปเดตล่าสุด: [วันที่]  
เวอร์ชัน: 1.0
```

#### 2.2 สร้างตาราง Privacy Policy Versions

```sql
CREATE TABLE privacy_policy_versions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    version VARCHAR(20) NOT NULL UNIQUE,
    content TEXT NOT NULL,
    effective_date DATE NOT NULL,
    created_by UUID REFERENCES users(id),
    is_active BOOLEAN DEFAULT false,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

COMMENT ON TABLE privacy_policy_versions IS 'เวอร์ชันของนโยบายความเป็นส่วนตัว (สำหรับตรวจสอบย้อนหลัง)';
```

---

### 3. ❌ **สิทธิของเจ้าของข้อมูล (Data Subject Rights)** - Priority: 🔴 สูง

**สถานะปัจจุบัน:** ❌ ไม่มีเลย

**สิทธิตาม PDPA ที่ต้องรองรับ:**

#### 3.1 สิทธิในการเข้าถึงข้อมูล (Right to Access)

**API Endpoint:**
```rust
// GET /api/me/export-data
// ดาวน์โหลดข้อมูลทั้งหมดของผู้ใช้ในรูปแบบ JSON/PDF
```

**ข้อมูลที่ส่งออก:**
```json
{
  "user_info": { ... },
  "student_info": { ... }, // ถ้ามี
  "staff_info": { ... }, // ถ้ามี
  "enrollment_history": [ ... ],
  "grade_history": [ ... ],
  "attendance_history": [ ... ],
  "consent_records": [ ... ],
  "audit_logs": [ ... ] // เฉพาะที่เกี่ยวข้องกับตนเอง
}
```

#### 3.2 สิทธิในการแก้ไขข้อมูล (Right to Rectification)

**สถานะ:** ⚠️ มีบางส่วน (สามารถแก้ไข profile ได้)

**ต้องปรับปรุง:**
- เพิ่ม UI ให้ชัดเจนว่าช่องไหนแก้ไขได้
- เพิ่มระบบขออนุมัติสำหรับข้อมูลที่ละเอียดอ่อน
- Log การแก้ไขทุกครั้ง

#### 3.3 สิทธิในการลบข้อมูล (Right to Erasure / Right to be Forgotten)

**API Endpoint:**
```rust
// POST /api/me/request-deletion
// ขอให้ลบข้อมูลส่วนบุคคล
```

**กระบวนการ:**
```
1. ผู้ใช้ส่งคำขอลบข้อมูล
2. ระบบตรวจสอบว่ามีข้อจำกัดทางกฎหมายหรือไม่
   - ถ้ามี: แจ้งผู้ใช้ว่าไม่สามารถลบได้ + เหตุผล
   - ถ้าไม่มี: ส่งคำขอไปยัง DPO/Admin
3. DPO/Admin พิจารณาและอนุมัติ
4. ระบบลบข้อมูล (หรือ anonymize)
5. แจ้งผู้ใช้เมื่อดำเนินการเสร็จสิ้น
```

**ตาราง Data Deletion Requests:**
```sql
CREATE TABLE data_deletion_requests (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id),
    request_type VARCHAR(50) NOT NULL, -- 'full_deletion', 'partial_deletion', 'anonymization'
    reason TEXT,
    status VARCHAR(50) NOT NULL DEFAULT 'pending', -- pending, approved, rejected, completed
    requested_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    reviewed_by UUID REFERENCES users(id),
    reviewed_at TIMESTAMPTZ,
    completed_at TIMESTAMPTZ,
    rejection_reason TEXT,
    metadata JSONB DEFAULT '{}'
);
```

#### 3.4 สิทธิในการถอนความยินยอม (Right to Withdraw Consent)

**UI:**
```
เมนู: ข้อมูลส่วนตัว → การจัดการความยินยอม

┌─────────────────────────────────────────────┐
│ ความยินยอมของคุณ                            │
├─────────────────────────────────────────────┤
│ ✅ การเก็บรวบรวมข้อมูลส่วนบุคคล             │
│    ให้ความยินยอมเมื่อ: 15 ม.ค. 2026         │
│    [ถอนความยินยอม]                          │
│                                             │
│ ✅ การใช้ข้อมูลเพื่อการติดต่อสื่อสาร         │
│    ให้ความยินยอมเมื่อ: 15 ม.ค. 2026         │
│    [ถอนความยินยอม]                          │
│                                             │
│ ❌ การรับข่าวสารและกิจกรรม                   │
│    ถอนความยินยอมเมื่อ: 20 ม.ค. 2026         │
│    [ให้ความยินยอมอีกครั้ง]                   │
└─────────────────────────────────────────────┘
```

#### 3.5 สิทธิในการโอนย้ายข้อมูล (Right to Data Portability)

**รูปแบบที่รองรับ:**
- JSON (มาตรฐาน, machine-readable)
- PDF (สำหรับผู้ใช้ทั่วไป)
- CSV (สำหรับข้อมูลตาราง)

---

### 4. ❌ **ระยะเวลาการเก็บข้อมูล (Data Retention Policy)** - Priority: 🔴 สูง

**สถานะปัจจุบัน:** ❌ ไม่มีการกำหนด

**ความเสี่ยง:**
- ผิด PDPA มาตรา 23 (7) - เก็บข้อมูลเกินความจำเป็น

**สิ่งที่ต้องทำ:**

#### 4.1 กำหนด Retention Period

```sql
CREATE TABLE data_retention_policies (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    entity_type VARCHAR(100) NOT NULL UNIQUE, -- 'user', 'student', 'staff', 'audit_log'
    retention_period_days INTEGER NOT NULL,
    retention_description TEXT NOT NULL,
    legal_basis TEXT, -- เหตุผลทางกฎหมาย
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- ตัวอย่างข้อมูล
INSERT INTO data_retention_policies (entity_type, retention_period_days, retention_description, legal_basis) VALUES
('student_active', 999999, 'นักเรียนที่กำลังศึกษา - เก็บตลอดระยะเวลาการศึกษา', 'สัญญาการศึกษา'),
('student_graduated', 1825, 'นักเรียนที่สำเร็จการศึกษา - เก็บ 5 ปี', 'ระเบียบกระทรวงศึกษาธิการ'),
('student_withdrawn', 1095, 'นักเรียนที่ลาออก - เก็บ 3 ปี', 'ระเบียบกระทรวงศึกษาธิการ'),
('staff_active', 999999, 'บุคลากรที่ปฏิบัติงาน - เก็บตลอดระยะเวลาการจ้าง', 'สัญญาจ้าง'),
('staff_resigned', 3650, 'บุคลากรที่ลาออก - เก็บ 10 ปี', 'พ.ร.บ.คุ้มครองแรงงาน'),
('audit_logs', 2555, 'Audit logs - เก็บ 7 ปี', 'มาตรฐาน ISO 27001'),
('consent_records', 999999, 'บันทึกความยินยอม - เก็บถาวร', 'หลักฐานการปฏิบัติตาม PDPA');
```

#### 4.2 Automated Data Cleanup Job

**สร้าง Cron Job:**
```rust
// backend-school/src/jobs/data_cleanup.rs

pub async fn cleanup_expired_data(pool: &PgPool) -> Result<(), Error> {
    // 1. ดึง retention policies
    // 2. หาข้อมูลที่หมดอายุ
    // 3. Anonymize หรือ Delete
    // 4. Log การดำเนินการ
}
```

#### 4.3 เพิ่ม Soft Delete

```sql
ALTER TABLE users 
    ADD COLUMN deleted_at TIMESTAMPTZ,
    ADD COLUMN deletion_reason TEXT,
    ADD COLUMN anonymized_at TIMESTAMPTZ;
```

**Anonymization:**
```sql
-- แทนที่จะลบ ให้ทำ Anonymization
UPDATE users
SET 
    national_id = 'ANONYMIZED_' || id,
    email = 'anonymized_' || id || '@example.com',
    first_name = 'Anonymized',
    last_name = 'User',
    phone = NULL,
    address = NULL,
    anonymized_at = NOW()
WHERE id = :user_id;
```

---

### 5. ⚠️ **มาตรการรักษาความปลอดภัย (Security Measures)** - Priority: 🟡 ปานกลาง

**สถานะปัจจุบัน:** ⚠️ มีพื้นฐาน แต่ต้องเสริม

#### ✅ สิ่งที่มีอยู่แล้ว (Good Points):

```
✅ Password hashing (bcrypt)
✅ JWT authentication
✅ Cookie-based sessions (HttpOnly, SameSite)
✅ Role-based access control (RBAC)
✅ Permission-based authorization
✅ Audit logging (พื้นฐาน)
✅ Multi-tenant isolation (แยก database per school)
```

#### ❌ สิ่งที่ยังขาด (Security Gaps):

##### 5.1 ไม่มี Rate Limiting
```rust
// ต้องเพิ่ม middleware
use tower::limit::RateLimitLayer;

// ป้องกัน brute-force attacks
app.layer(RateLimitLayer::new(
    100, // 100 requests
    std::time::Duration::from_secs(60) // per minute
))
```

##### 5.2 ไม่มี HTTPS Enforcement
```rust
// ควรบังคับ HTTPS ใน production
if !request.uri().scheme_str().unwrap_or("") == "https" {
    return Err(StatusCode::FORBIDDEN);
}
```

##### 5.3 ข้อมูลส่วนบุคคลที่ละเอียดอ่อนไม่ได้เข้ารหัส

**ควรเข้ารหัสในฐานข้อมูล:**
```sql
-- ข้อมูลที่ควร encrypt:
- national_id (เลขบัตรประชาชน)
- medical_conditions (ข้อมูลสุขภาพ)

-- ใช้ pgcrypto extension
CREATE EXTENSION IF NOT EXISTS pgcrypto;

-- ตัวอย่าง
INSERT INTO users (national_id) 
VALUES (pgp_sym_encrypt('1234567890123', 'encryption_key'));
```

##### 5.4 ไม่มี Input Sanitization
```rust
// ต้องเพิ่ม input validation และ sanitization
use validator::Validate;

#[derive(Validate)]
struct CreateUserRequest {
    #[validate(length(min = 1, max = 100))]
    first_name: String,
    
    #[validate(email)]
    email: String,
    
    #[validate(length(equal = 13), custom = "validate_thai_national_id")]
    national_id: String,
}
```

##### 5.5 ไม่มี CSRF Protection
```rust
// เพิ่ม CSRF token สำหรับ forms
use axum_csrf::{CsrfConfig, CsrfLayer};
```

##### 5.6 ไม่มี Security Headers
```rust
// เพิ่ม security headers
use tower_http::set_header::SetResponseHeaderLayer;

app.layer(SetResponseHeaderLayer::overriding(
    header::STRICT_TRANSPORT_SECURITY, 
    "max-age=31536000; includeSubDomains".parse().unwrap()
))
.layer(SetResponseHeaderLayer::overriding(
    header::X_CONTENT_TYPE_OPTIONS,
    "nosniff".parse().unwrap()
))
.layer(SetResponseHeaderLayer::overriding(
    header::X_FRAME_OPTIONS,
    "DENY".parse().unwrap()
));
```

---

### 6. ⚠️ **Audit Logging** - Priority: 🟡 ปานกลาง

**สถานะปัจจุบัน:** ⚠️ มีตาราง แต่ไม่ได้ใช้งาน

**จากโค้ด:**
```rust
// src/utils/audit.rs
//! Audit logging system - prepared for future use
//! TODO: Integrate audit logging into handlers (staff, roles, etc.)
```

**ต้องดำเนินการ:**

#### 6.1 เพิ่ม Audit Logging ในทุก Handler

**การกระทำที่ต้อง log:**
```
✅ Create user/student/staff
✅ Update sensitive data (national_id, salary, medical_info)
✅ Delete user
✅ Login/Logout
✅ Failed login attempts
✅ Change password
✅ Update consent
✅ Access sensitive data
✅ Export data
✅ Permission changes
```

**ตัวอย่าง:**
```rust
// เมื่อสร้าง staff
audit::log_create(
    &pool,
    current_user_id,
    "staff",
    new_staff_id,
    Some(format!("{} {}", first_name, last_name)),
    serde_json::to_value(&staff_data)?
).await?;

// เมื่อแก้ไขข้อมูลที่ละเอียดอ่อน
audit::log_update(
    &pool,
    current_user_id,
    "staff",
    staff_id,
    Some(staff_name),
    old_values,
    new_values,
    changes
).await?;
```

#### 6.2 เพิ่ม Audit Log Viewer (UI)

**หน้า: `/admin/audit-logs`**

```
Filters:
- วันที่ (Date range)
- ผู้ใช้ (User)
- การกระทำ (Action)
- ประเภทข้อมูล (Entity type)

Columns:
- วันเวลา
- ผู้ดำเนินการ
- การกระทำ
- ข้อมูลที่ได้รับผลกระทบ
- รายละเอียด
- IP Address
```

---

### 7. ❌ **Data Breach Notification Plan** - Priority: 🟡 ปานกลาง

**สถานะปัจจุบัน:** ❌ ไม่มีเลย

**ตาม PDPA มาตรา 37:**
- หากมีการละเมิดข้อมูล ต้องรายงานต่อสำนักงาน ปปช. ภายใน 72 ชั่วโมง
- ต้องแจ้งเจ้าของข้อมูลที่ได้รับผลกระทบ

**สิ่งที่ต้องเตรียม:**

#### 7.1 Data Breach Response Plan

```markdown
# แผนรับมือการรั่วไหลของข้อมูล

## ขั้นตอนที่ 1: ตรวจจับและประเมิน (0-24 ชม.)
- ตรวจสอบขอบเขตการรั่วไหล
- ระบุข้อมูลที่รั่วไหล
- ประเมินความรุนแรง

## ขั้นตอนที่ 2: แก้ไขและป้องกัน (0-48 ชม.)
- ปิดช่องโหว่
- เปลี่ยน credentials
- Backup ข้อมูล

## ขั้นตอนที่ 3: รายงาน (0-72 ชม.)
- รายงานต่อ PDPC (สำนักงาน ปปช.)
- แจ้งเจ้าของข้อมูลที่ได้รับผลกระทบ

## ขั้นตอนที่ 4: บทเรียน
- ทบทวนสาเหตุ
- ปรับปรุงมาตรการความปลอดภัย
```

#### 7.2 Breach Detection Monitoring

```rust
// ตัวอย่างการตรวจจับพฤติกรรมผิดปกติ
- Failed login attempts > 5 times in 5 minutes
- Bulk data export
- Access to data outside normal hours
- Multiple users accessing from same IP
```

---

### 8. ❌ **Data Processing Agreement (DPA)** - Priority: 🟡 ปานกลาง

**หากระบบใช้บริการจากบุคคลที่สาม:**

**ควรมี DPA กับ:**
- Cloud Provider (AWS, Google Cloud, Azure)
- Email Service (SendGrid, etc.)
- Analytics (Google Analytics)
- Payment Gateway

---

## 📋 สรุปรายการที่ต้องทำ (Action Items)

### Priority 🔴 สูงสุด (Critical) - ทำก่อน

- [ ] **1. ระบบความยินยอม (Consent Management)**
  - [ ] เพิ่มตาราง `consent_records`
  - [ ] สร้าง API endpoints สำหรับ consent
  - [ ] เพิ่ม Consent Form ในหน้าลงทะเบียน/Login
  - [ ] เพิ่มหน้าจัดการความยินยอม (User dashboard)
  - Effort: 5-7 days

- [ ] **2. นโยบายความเป็นส่วนตัว (Privacy Policy)**
  - [ ] เขียน Privacy Policy ตามมาตรา 23
  - [ ] สร้างหน้า `/privacy-policy`
  - [ ] สร้างตาราง `privacy_policy_versions`
  - [ ] เพิ่ม link ในทุกหน้าที่เกี่ยวข้อง
  - Effort: 3-5 days

- [ ] **3. สิทธิของเจ้าของข้อมูล (Data Subject Rights)**
  - [ ] Right to Access: API export ข้อมูล
  - [ ] Right to Erasure: ระบบขอลบข้อมูล
  - [ ] Right to Withdraw Consent: หน้า UI จัดการความยินยอม
  - [ ] Right to Data Portability: Export JSON/PDF
  - [ ] สร้างตาราง `data_deletion_requests`
  - Effort: 7-10 days

- [ ] **4. ระยะเวลาการเก็บข้อมูล (Data Retention)**
  - [ ] สร้างตาราง `data_retention_policies`
  - [ ] กำหนด retention period สำหรับแต่ละประเภทข้อมูล
  - [ ] สร้าง automated cleanup job
  - [ ] เพิ่ม soft delete และ anonymization
  - Effort: 5-7 days

**รวม Priority 🔴: 20-29 วัน (3-4 สัปดาห์)**

---

### Priority 🟡 สูง (High) - ทำถัดไป

- [ ] **5. ปรับปรุงความปลอดภัย**
  - [ ] เพิ่ม Rate Limiting
  - [ ] บังคับ HTTPS
  - [ ] เข้ารหัสข้อมูลที่ละเอียดอ่อน (pgcrypto)
  - [ ] เพิ่ม Input Validation
  - [ ] เพิ่ม CSRF Protection
  - [ ] เพิ่ม Security Headers
  - Effort: 5-7 days

- [ ] **6. Audit Logging (ใช้งานจริง)**
  - [ ] Integrate audit logging ในทุก handlers
  - [ ] เพิ่มหน้า Audit Log Viewer
  - [ ] เพิ่ม alerts สำหรับ suspicious activities
  - Effort: 3-5 days

- [ ] **7. Training และ Documentation**
  - [ ] อบรม Admin/DPO เรื่อง PDPA
  - [ ] สร้างคู่มือการใช้งานระบบสิทธิ PDPA
  - [ ] เอกสารแนวทางปฏิบัติสำหรับเจ้าหน้าที่
  - Effort: 3-5 days

**รวม Priority 🟡: 11-17 วัน (2-3 สัปดาห์)**

---

### Priority 🟢 ปานกลาง (Medium) - ทำทีหลัง

- [ ] **8. Data Breach Response Plan**
  - [ ] เขียนแผนรับมือการละเมิดข้อมูล
  - [ ] ตั้งทีมรับผิดชอบ
  - [ ] สร้างระบบตรวจจับการรั่วไหล
  - Effort: 3-5 days

- [ ] **9. Data Processing Agreements**
  - [ ] ทำ DPA กับ Cloud Provider
  - [ ] ทำ DPA กับบริการภายนอกทั้งหมด
  - Effort: 2-3 days

- [ ] **10. Data Protection Impact Assessment (DPIA)**
  - [ ] ประเมินความเสี่ยงของการประมวลผลข้อมูล
  - Effort: 3-5 days

**รวม Priority 🟢: 8-13 วัน (1-2 สัปดาห์)**

---

## 📅 Timeline แนะนำ

```
Month 1: PDPA Core Compliance (Priority 🔴)
  Week 1-2: Consent Management + Privacy Policy
  Week 3-4: Data Subject Rights + Data Retention

Month 2: Security Hardening (Priority 🟡)
  Week 1-2: Security Improvements
  Week 3: Audit Logging
  Week 4: Training & Documentation

Month 3: Advanced Compliance (Priority 🟢)
  Week 1: Data Breach Plan
  Week 2: DPAs
  Week 3-4: DPIA + Review

Total: 3 เดือน เพื่อ PDPA Compliance ที่สมบูรณ์
```

---

## ⚠️ ความเสี่ยงทางกฎหมาย

### มาตราที่อาจฝ่าฝืน:

| มาตรา | เนื้อหา | สถานะ | โทษ |
|-------|---------|-------|-----|
| มาตรา 19 | การเก็บรวบรวมข้อมูลต้องได้รับความยินยอม | ❌ ไม่มีระบบความยินยอม | ปรับไม่เกิน 5 ล้านบาท |
| มาตรา 23 | ต้องแจ้งรายละเอียดการเก็บข้อมูล | ❌ ไม่มี Privacy Policy | ปรับไม่เกิน 3 ล้านบาท |
| มาตรา 27 | เจ้าของข้อมูลมีสิทธิ์ขอเข้าถึงข้อมูล | ❌ ไม่มีกลไก | ปรับไม่เกิน 1 ล้านบาท |
| มาตรา 37 | รายงานการละเมิดข้อมูลภายใน 72 ชม. | ❌ ไม่มีแผน | ปรับไม่เกิน 5 ล้านบาท |

**รวมความเสี่ยง: สูงสุด 14 ล้านบาท**

---

## ✅ ข้อแนะนำสุดท้าย

### 1. เริ่มจาก Priority 🔴 ก่อน
เพราะเป็นข้อกำหนดพื้นฐานของ PDPA ที่หลีกเลี่ยงไม่ได้

### 2. แต่งตั้ง Data Protection Officer (DPO)
ควรมีเจ้าหน้าที่คุ้มครองข้อมูลส่วนบุคคลที่ชัดเจน

### 3. อบรมพนักงานทุกคน
ให้ความรู้เรื่อง PDPA และวิธีปฏิบัติที่ถูกต้อง

### 4. Review policies ทุก 6 เดือน
เพื่อให้ทันกับการเปลี่ยนแปลงของกฎหมาย

### 5. จ้างที่ปรึกษากฎหมาย
สำหรับ Privacy Policy และ DPAs

---

## 📞 ติดต่อสำนักงาน PDPC

**สำนักงานคณะกรรมการคุ้มครองข้อมูลส่วนบุคคล (PDPC Thailand)**  
เว็บไซต์: https://www.pdpc.or.th  
โทร: 0-2141-6993  
อีเมล: saraban@pdpc.or.th

---

**วันที่จัดทำ:** 8 มกราคม 2026  
**ผู้จัดทำ:** System Legal Compliance Review  
**อัปเดตครั้งถัดไป:** เมื่อมีการเปลี่ยนแปลงกฎหมายหรือระบบ
